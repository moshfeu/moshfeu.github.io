{
  "scripts": {
    "start": "bundle exec jekyll serve",
    "generate-tags": "sh node_modules/github-pages-tags/precommit.sh"
  },
  "pre-commit": [
    "generate-tags"
  ],
  "dependencies": {
    "github-pages-tags": "^1.0.0"
  }
}
